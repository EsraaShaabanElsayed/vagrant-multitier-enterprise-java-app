apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: daemonset
  labels:
    app: la
spec:
  selector:
    matchLabels:
      app: la
  template:
    metadata:
      labels:
        app: la
    spec:
      serviceAccountName: sa
      nodeSelector:
        kubernetes.io/os: linux
      containers:
        - name: container
          image: registry.access.redhat.com/ubi8/ubi-minimal:latest
          command: ["/bin/bash", "-c"]
          args:
            - >
              echo 'Running with user ID:' && id -u &&
              echo 'User info:' && id && sleep 3600
          securityContext:
            runAsUser: 0
            runAsGroup: 0
      hostIPC: false
      hostPID: false
 